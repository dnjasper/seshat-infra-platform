name: Infrastructure Pipeline

on:
  push:
    branches:
        - main

jobs:
  infrastructure:
    name: Create Cloud Infrastructure 
    runs-on: ubuntu-latest
    permissions:
        id-token: write
        contents: read
    defaults:
      run:
        working-directory: ./environments/dev/platform
    env:
      TF_VAR_identity_private_key: ${{ secrets.FLUX_IDENTITY_PRIVATE_KEY }}
      TF_VAR_identity_public_key: ${{ secrets.FLUX_IDENTITY_PUBLIC_KEY }}
      TF_VAR_known_hosts: ${{ secrets.FLUX_KNOWN_HOSTS }}
      TF_VAR_github_token: ${{ secrets.GH_TOKEN }}


    steps:
    - name: Create Repository
      uses: actions/checkout@v4


    - name: Debug GitHub identity
      run: |
        echo "Role: ${{ vars.AWS_ROLE_TO_ASSUME }}"
        echo "Repository: ${{ github.repository }}"
        echo "Ref: ${{ github.ref }}"
        echo "Event: ${{ github.event_name }}"
        echo "Workflow: ${{ github.workflow }}"

    - name: Debug AWS role
      run: |
        echo "AWS_ROLE_TO_ASSUME is set: ${{ vars.AWS_ROLE_TO_ASSUME != '' }}"
        echo "AWS_ROLE_TO_ASSUME: ${{ vars.AWS_ROLE_TO_ASSUME }}"

    # - name: Debug GitHub OIDC
    #   shell: bash
    #   run: |
    #     set -euo pipefail

    #     echo "URL present: ${ACTIONS_ID_TOKEN_REQUEST_URL:+yes}"
    #     echo "Token present: ${ACTIONS_ID_TOKEN_REQUEST_TOKEN:+yes}"

    #     RESPONSE=$(curl -sS \
    #       -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
    #       "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com")

    #     TOKEN=$(echo "$RESPONSE" | jq -r '.value // empty')

    #     if [ -z "$TOKEN" ]; then
    #       echo "::error::GitHub did not return an OIDC token"
    #       echo "$RESPONSE" | jq .
    #       exit 1
    #     fi

    #     echo "OIDC token received"

    #     export TOKEN

    #     python3 <<'PY'
    #     import os
    #     import json
    #     import base64

    #     token = os.environ["TOKEN"]
    #     payload = token.split(".")[1]
    #     payload += "=" * (-len(payload) % 4)

    #     claims = json.loads(base64.urlsafe_b64decode(payload))

    #     print("OIDC subject:", claims.get("sub"))
    #     print("OIDC audience:", claims.get("aud"))
    #     print("OIDC issuer:", claims.get("iss"))
    #     print("OIDC repository:", claims.get("repository"))
    #     print("OIDC ref:", claims.get("ref"))
    #     PY
    # - name: Dump OIDC token claims
    #   shell: bash
    #   run: |
    #     echo "Requesting OIDC token..."

    #     TOKEN=$(curl -s \
    #       -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
    #       "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com" | jq -r '.value')

    #     echo "Token received." 

    #     echo "$TOKEN" | cut -d '.' -f2 | base64 -d 2>/dev/null | jq .

    # - name: Access AWS
    #   uses: aws-actions/configure-aws-credentials@v4
    #   with:
    #     role-to-assume: arn:aws:iam::468402787427:role/github_actions_role
    #     aws-region: ${{ secrets.AWS_REGION }}
    #     audience: sts.amazonaws.com
    
    # - name: Create GitHub App token
    #   id: github-app-token
    #   uses: actions/create-github-app-token@v2
    #   with:
    #    app-id: ${{ vars.APPID }}
    #    private-key: ${{ secrets.SESHATTHIRDREPOACCESS }}
    #    owner: dnjasper
    #   # appId: ${{ vars.APPID }}
    #    repositories: seshat-workload-platform

    # - name: Dump OIDC token claims
    #   shell: bash
    #   run: |
    #     echo "Requesting OIDC token..."

    #     TOKEN=$(curl -s \
    #       -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
    #       "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com" | jq -r '.value')

    #     echo "Token received." 

    #     echo "$TOKEN" | cut -d '.' -f2 | base64 -d 2>/dev/null | jq .
    - name: Debug GitHub identity
      run: |
        echo "Role: ${{ vars.AWS_ROLE_TO_ASSUME }}"
        echo "Repository: ${{ github.repository }}"
        echo "Ref: ${{ github.ref }}"
        echo "Event: ${{ github.event_name }}"
        echo "Workflow: ${{ github.workflow }}"

    - name: Access AWS
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ vars.AWS_ROLE_TO_ASSUME }}
          # FIX: Add this line so GitHub knows where to point the deployment.
        aws-region: us-east-1  


    


    - name: Establish Terraform   
      uses: hashicorp/setup-terraform@v4
      with:
        terraform_version: 1.6.2
        terraform_wrapper: false
    - name: Verify Terraform Code Syntax
      run: terraform init -upgrade && terraform validate

    # - name: Verify Flux secrets are available
    #   shell: bash
    #   run: |
    #     if [[ -n "$TF_VAR_identity_private_key" ]]; then
    #       echo "identity_private_key: SET"
    #     else
    #       echo "identity_private_key: EMPTY"
    #     fi

    #     if [[ -n "$TF_VAR_identity_public_key" ]]; then
    #       echo "identity_public_key: SET"
    #     else
    #       echo "identity_public_key: EMPTY"
    #     fi

    #     if [[ -n "$TF_VAR_known_hosts" ]]; then
    #       echo "known_hosts: SET"
    #     else
    #       echo "known_hosts: EMPTY"
    #     fi






    - name: Check Flux variable lengths
      shell: bash
      run: |
        echo "private key length: ${#TF_VAR_identity_private_key}"
        echo "public key length: ${#TF_VAR_identity_public_key}"
        echo "known_hosts length: ${#TF_VAR_known_hosts}"

    #- name: Terraform Plan
      #run: terraform plan -replace='module.flux.kubernetes_secret_v1.flux-git-deploy'

    - name: Deploy Code into InfraS
      # env:
      #   TF_VAR_github_token: ${{ secrets.MY_GITHUB_PAT }}
      run: terraform plan
    
    - name: Apply Code into InfraS
      run: terraform apply -auto-approve
     # run: terraform apply -target=module.github-actions -target=module.vpc -target=module.ecr -target=module.eks -auto-approve
       
    # - name: Push Remaining IRSA
    #   run: terraform apply -target=module.irsa -auto-approve

    # - name: Push Remaining Secret
    #   run: terraform apply -target=module.flux -auto-approve